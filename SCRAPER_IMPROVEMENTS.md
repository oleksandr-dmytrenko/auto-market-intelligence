# Улучшения Bidfax Scraper

## ✅ Реализованные улучшения

### 1. Не-headless режим
- ✅ Добавлена поддержка видимого режима Chrome через переменную окружения `CHROME_HEADLESS=false`
- ✅ По умолчанию используется headless режим для production
- ✅ Видимый режим лучше обходит Cloudflare, но требует дисплей (для локальной разработки)

**Использование:**
```bash
export CHROME_HEADLESS=false
docker compose up python_workers
```

### 2. Поддержка прокси
- ✅ Добавлена полная поддержка HTTP/HTTPS прокси
- ✅ Поддержка прокси с авторизацией
- ✅ Настройка через переменные окружения:
  - `PROXY_HOST` - адрес прокси сервера
  - `PROXY_PORT` - порт прокси
  - `PROXY_USERNAME` - имя пользователя (опционально)
  - `PROXY_PASSWORD` - пароль (опционально)

**Использование:**
```bash
export PROXY_HOST=proxy.example.com
export PROXY_PORT=8080
export PROXY_USERNAME=user
export PROXY_PASSWORD=pass
docker compose up python_workers
```

### 3. Улучшенная логика обхода Cloudflare

#### 3.1 Улучшенное обнаружение Cloudflare
- ✅ Проверка заголовка страницы (403, Forbidden)
- ✅ Проверка текста страницы (расширенный список индикаторов)
- ✅ Проверка iframe элементов
- ✅ Проверка DOM элементов с Cloudflare классами/ID
- ✅ Проверка JavaScript challenge элементов

#### 3.2 Умное ожидание обхода
- ✅ Автоматическое ожидание до 90 секунд (увеличено с 30)
- ✅ Периодическая проверка статуса каждые 3 секунды
- ✅ Автоматическое обновление страницы каждые 30 секунд при застревании
- ✅ Прогресс-индикатор каждые 15 секунд
- ✅ Проверка загрузки реального контента после обхода

#### 3.3 Верификация контента
- ✅ Проверка что загружен реальный контент, а не challenge
- ✅ Проверка размера HTML (Cloudflare challenge обычно < 5KB)
- ✅ Проверка наличия ссылок и элементов на странице
- ✅ Проверка заголовка страницы (не должен содержать "403", "Forbidden", "Error")

#### 3.4 Retry логика
- ✅ Автоматические повторные попытки при застревании
- ✅ Обновление страницы при необходимости
- ✅ Альтернативный подход если основной не сработал

### 4. Дополнительные улучшения Chrome

- ✅ Кастомный User-Agent через `CHROME_USER_AGENT`
- ✅ Оптимизированные опции Chrome для обхода детекции
- ✅ CDP команды для скрытия webdriver индикаторов
- ✅ Улучшенные размеры окна браузера

## 📁 Измененные файлы

1. **`python_workers/workers/auction_fetcher.py`**
   - Добавлена поддержка прокси
   - Добавлена поддержка не-headless режима
   - Улучшены опции Chrome
   - Добавлены CDP команды для обхода детекции

2. **`python_workers/workers/bidfax_scraper.py`**
   - Полностью переработана логика обхода Cloudflare
   - Добавлены методы `_wait_for_cloudflare_bypass()`, `_wait_for_content_load()`, `_verify_content_loaded()`
   - Улучшена проверка Cloudflare challenge
   - Добавлена retry логика

3. **`docker-compose.yml`**
   - Добавлены переменные окружения для конфигурации скрапера
   - Поддержка прокси настроек
   - Поддержка headless/visible режима

4. **`SCRAPER_CONFIG.md`** (новый)
   - Документация по конфигурации скрапера
   - Примеры использования
   - Рекомендации

## 🚀 Как использовать

### Базовый запуск (headless)
```bash
docker compose up python_workers
```

### С видимым браузером (для локальной разработки)
```bash
export CHROME_HEADLESS=false
docker compose up python_workers
```

### С прокси
```bash
export PROXY_HOST=proxy.example.com
export PROXY_PORT=8080
docker compose up python_workers
```

### Комбинированный запуск
```bash
export CHROME_HEADLESS=false
export PROXY_HOST=proxy.example.com
export PROXY_PORT=8080
export PROXY_USERNAME=user
export PROXY_PASSWORD=pass
docker compose up python_workers
```

### Через .env файл
Создайте `.env` файл:
```env
CHROME_HEADLESS=false
PROXY_HOST=proxy.example.com
PROXY_PORT=8080
PROXY_USERNAME=user
PROXY_PASSWORD=pass
```

## ⚠️ Важные замечания

1. **Docker контейнеры**: Видимый режим (`CHROME_HEADLESS=false`) не будет работать в Docker контейнере без настройки X11 forwarding. Для Docker используйте headless режим.

2. **Cloudflare**: Несмотря на все улучшения, Cloudflare может все еще блокировать запросы. В этом случае:
   - Используйте качественные residential прокси
   - Попробуйте увеличить время ожидания
   - Используйте ротацию прокси

3. **Прокси**: Для лучших результатов используйте residential прокси, а не datacenter прокси.

4. **Rate Limiting**: Скрапер автоматически контролирует частоту запросов через Redis.

## 📊 Результаты тестирования

- ✅ Chrome Driver инициализируется успешно
- ✅ Прокси настройки применяются корректно
- ✅ Логика обхода Cloudflare работает (обнаруживает challenge)
- ✅ Улучшенное ожидание и retry логика функционируют
- ⚠️ Cloudflare все еще может блокировать (это нормально для строгой защиты)

## 🔄 Следующие шаги (опционально)

1. Добавить поддержку ротации прокси
2. Добавить поддержку cookies для обхода Cloudflare
3. Интеграция с сервисами обхода Cloudflare (например, 2captcha)
4. Добавить метрики и мониторинг успешности обхода




