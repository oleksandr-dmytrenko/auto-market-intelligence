# Обход Incapsula (Cloudflare Enterprise)

## 📋 Обзор

Incapsula - это продвинутая система защиты от ботов, используемая многими сайтами, включая IAAI.com. Это более сложная система, чем стандартный Cloudflare, и требует специальных техник обхода.

## ✅ Реализованные улучшения

### 1. Selenium Stealth
- ✅ Добавлена библиотека `selenium-stealth` для улучшенного обхода детекции
- ✅ Автоматическое скрытие признаков автоматизации
- ✅ Эмуляция реального браузера

### 2. Улучшенные Stealth скрипты
- ✅ Скрытие `navigator.webdriver`
- ✅ Эмуляция плагинов браузера
- ✅ Эмуляция `window.chrome` объекта
- ✅ Эмуляция WebGL параметров
- ✅ Эмуляция connection API
- ✅ Продвинутая эмуляция navigator свойств

### 3. Fake User-Agent
- ✅ Использование библиотеки `fake-useragent` для генерации реалистичных User-Agents
- ✅ Ротация User-Agents для каждого запроса

### 4. Улучшенная обработка Challenge
- ✅ Увеличенное время ожидания (до 90 секунд)
- ✅ Имитация человеческого поведения (скроллинг, движения мыши)
- ✅ Периодическая проверка статуса challenge
- ✅ Детекция множественных индикаторов Incapsula

### 5. CDP команды
- ✅ Использование Chrome DevTools Protocol для эмуляции реального браузера
- ✅ Переопределение User-Agent через CDP
- ✅ Дополнительные скрипты для обхода детекции

## 🔧 Использование

### Установка зависимостей

```bash
pip install -r requirements.txt
```

Новые зависимости:
- `selenium-stealth==1.0.6` - улучшенный обход детекции
- `fake-useragent==1.0.0` - генерация реалистичных User-Agents

### Настройка

Скрапер автоматически применяет все улучшения при инициализации драйвера.

### Переменные окружения

```bash
# Отключить headless режим (лучше для обхода)
CHROME_HEADLESS=false

# Использовать прокси (рекомендуется для Incapsula)
PROXY_HOST=your-proxy-host
PROXY_PORT=your-proxy-port
PROXY_USERNAME=your-username  # опционально
PROXY_PASSWORD=your-password  # опционально
```

## 🎯 Техники обхода

### 1. Stealth скрипты

Скрипты выполняются на каждой новой странице и скрывают признаки автоматизации:

```javascript
// Скрытие webdriver
Object.defineProperty(navigator, 'webdriver', { get: () => undefined });

// Эмуляция плагинов
Object.defineProperty(navigator, 'plugins', { ... });

// Эмуляция chrome объекта
window.chrome = { runtime: {}, ... };
```

### 2. Имитация человеческого поведения

- Прокрутка страницы
- Движения мыши
- Случайные задержки
- Периодическая активность во время ожидания challenge

### 3. Ожидание Challenge

Скрапер автоматически:
- Определяет challenge страницу по множественным индикаторам
- Ждет до 90 секунд для прохождения challenge
- Имитирует активность во время ожидания
- Проверяет успешность обхода

## ⚠️ Ограничения

1. **Residential прокси**: Для надежного обхода Incapsula рекомендуется использовать residential прокси, так как datacenter прокси часто блокируются.

2. **Headless режим**: Headless режим легче детектируется. Рекомендуется использовать видимый режим (`CHROME_HEADLESS=false`), но это требует X server в Docker.

3. **Rate limiting**: Даже с обходом, важно соблюдать вежливые задержки между запросами.

4. **Обновления защиты**: Incapsula регулярно обновляется, поэтому техники обхода могут потребовать обновления.

## 📊 Индикаторы Challenge

Скрапер проверяет следующие индикаторы Incapsula/Cloudflare:

- `checking your browser`
- `just a moment`
- `incapsula`
- `_incapsula_resource`
- `ddos protection`
- `cf-browser-verification`
- `challenge-platform`
- `ray id`
- `cf_clearance`
- `__cf_bm`
- `access denied`
- `403 forbidden`

## 🚀 Рекомендации

1. **Используйте residential прокси** для максимальной эффективности
2. **Отключите headless режим** если возможно
3. **Увеличьте задержки** между запросами
4. **Мониторьте успешность** обхода и адаптируйте параметры
5. **Рассмотрите использование API** если доступно

## 📝 Пример использования

```python
from workers.auction_fetcher import AuctionFetcher
from workers.rate_limiter import RateLimiter
import redis

redis_client = redis.from_url('redis://localhost:6379/0')
rate_limiter = RateLimiter(redis_client)
fetcher = AuctionFetcher(rate_limiter)

# Драйвер автоматически настроен с улучшениями для обхода Incapsula
vehicles = fetcher.fetch_iaai_listings({'make': 'Toyota', 'model': 'Camry'}, limit=10)
```

## 🔄 Обновления

При обновлении защиты Incapsula может потребоваться:
- Обновление stealth скриптов
- Изменение параметров ожидания
- Использование новых техник обхода

Следите за изменениями в поведении скрапера и адаптируйте код при необходимости.

